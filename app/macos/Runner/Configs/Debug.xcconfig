#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"

// Local debug builds: skip code signing so we don't need a real
// developer team / cert in the keychain. Production builds keep
// the AppInfo.xcconfig signing settings.
CODE_SIGN_IDENTITY = -
CODE_SIGNING_REQUIRED = NO
CODE_SIGNING_ALLOWED = NO
DEVELOPMENT_TEAM =
