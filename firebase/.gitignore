# Firebase CLI / emulator state
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
pubsub-debug.log
auth-debug.log
*-debug.log

# Persisted emulator state (auto-export/import for `npm run dev`)
.emulator-data/

# Local-only overrides
*.local
.runtimeconfig.json

# Service account credentials must NEVER be committed.
service-account*.json
*-firebase-adminsdk-*.json
